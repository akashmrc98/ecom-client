import { ReviewLikesPipe } from './review-likes.pipe';

describe('ReviewLikesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReviewLikesPipe();
    expect(pipe).toBeTruthy();
  });
});

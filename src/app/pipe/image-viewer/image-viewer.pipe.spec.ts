import { ImageViewerPipe } from './image-viewer.pipe';

describe('ImageViewerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageViewerPipe();
    expect(pipe).toBeTruthy();
  });
});
